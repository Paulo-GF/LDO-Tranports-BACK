# Initialize environment variables
export SQITCH_USERNAME=ldo
export SQITCH_PASSWORD=ldo

# Sqitch revert
sqitch revert db:pg:ldo

# Disable environment variables
unset SQITCH_USERNAME
unset SQITCH_PASSWORD